
%header.tc.pv4.pv5-ns
	%img{:src => "#{@user.avatar.url(:medium)}", :alt => "#{@user.username}", class: 'br-100 pa1 ba b--black-10 h3 w3'}/
	%h1.f5.f4-ns.fw6.mid-gray= @user.username
	%h2.f6.gray.fw2.ttu.tracked
		%span= "#{@user.followers.count} Followers"
		%span= "#{@user.followed_users.count} Following"
		%span
			- if user_signed_in? && current_user.can_follow?(@user)
				= follow_button @user


%section.mw5.mw7-ns.center.pa3.ph5-ns
	= render @posts

