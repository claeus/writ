= div_for @post do
	%article.pa3.pa5-ns.ph7-l.georgia.mw9-l.center
		- if @post.photo?
			%img{:src => "#{@post.photo.url}", :alt => "#{@post.title}", class: 'w-100 f5 measure'}/
		.f2.f1-m.f-headline-l.measure-narrow.lh-title.mv0= @post.title
		%small= time_ago_in_words(@post.created_at) + " ago"
		%span= link_to "#{@post.user.username}", @post.user
		%h1= social_share_button_tag(@post.title, desc: "#{@post.body.truncate(40)}")
		
		.lh-copy= @post.body.html_safe

